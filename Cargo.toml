[workspace]
members = ["src/libkrun", "src/krun_input"]
exclude = ["examples/gtk_display", "virtio-gpu-nv"]
resolver = "2"

[profile.dev]
#panic = "abort"

[profile.release]
#panic = "abort"
lto = true
